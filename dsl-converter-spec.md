# DSL2DesignSpace Converter - Полная спецификация для реализации

## 1. ОБЗОР ПРОЕКТА

### Цель
Создать двунаправленный конвертер между компактным DSL (Domain-Specific Language) форматом и стандартным fontTools .designspace XML форматом для работы с вариативными шрифтами.

### Проблема которую решаем
- .designspace файлы очень многословны (тысячи строк XML)
- Сложно быстро понять структуру шрифтового семейства
- Много повторяющейся информации
- Трудно вручную создавать и редактировать

### Решение
Компактный DSL формат, который:
- Читаемый и понятный (50-100 строк вместо 1000+)
- Поддерживает стандартные значения по умолчанию
- Умеет автогенерировать повторяющиеся элементы
- Показывает реальную структуру (какие мастера есть, что интерполируется)

## 2. КЛЮЧЕВЫЕ КОНЦЕПЦИИ

### User Space vs Design Space
```
User Space = значения которые видит пользователь (font-weight: 400)
Design Space = координаты где находятся мастера для интерполяции

Пример маппинга:
400 Regular > 125  означает:
- Пользователь запрашивает font-weight: 400 (Regular)
- Мастер находится в точке 125 в design space
```

### Разреженные матрицы мастеров
```
Полная матрица: weight(8) × width(6) × italic(2) = 96 мастеров
Оптимизированная: только углы + центр = 12 мастеров
Экономия: 87%
```

### Стандартные веса (встроенные дефолты)
```python
STANDARD_WEIGHTS = {
    100: 'Thin',
    200: 'ExtraLight', 
    300: 'Light',
    400: 'Regular',
    500: 'Medium',
    600: 'SemiBold',
    700: 'Bold',
    800: 'ExtraBold',
    900: 'Black'
}
```

## 3. ФОРМАТЫ DSL (от простого к сложному)

### 3.1 Базовый формат
```dsl
family MyFont
suffix VF

axes
    weight wght 100:400:900
        100 Thin    > 0
        400 Regular > 400
        900 Black   > 1000
    
    italic ital binary  # 0/1

masters
    Thin        [0, 0]
    Regular     [400, 0] @base
    Black       [1000, 0]
    ThinItalic  [0, 1]
    Italic      [400, 1] @base
    BlackItalic [1000, 1]

instances auto  # генерировать все комбинации
```

### 3.2 Компактный формат
```dsl
family MyFont

# Используем стандартные значения
weight 100:400:900  # автоматически Thin, Regular, Black
italic binary

masters 3×2  # автоматически найти по паттернам имён
```

### 3.3 Расширенный формат с переопределениями
```dsl
family MyFont

axes
    weight 100:400:900
        # Переопределяем только отличия от стандарта
        100 > 0     # Thin по умолчанию, меняем только design space
        900 > 1000  # Black по умолчанию
        
        # Добавляем нестандартные
        350 Book > 350

masters
    # Явное указание путей
    Thin        @sources/MyThin.ufo
    Regular     @sources/Regular_v2.ufo @base
    
    # Автопоиск по паттерну
    scan *.ufo

rules
    # Правила подстановки
    dollar > dollar.alt @ weight >= 600
```

### 3.4 Математический формат
```dsl
weight mapping curve: cubic-bezier(0.42, 0, 0.58, 1)
# или точки для сплайна
weight 100:400:900
    100 > 0    # 0%
    200 > 132  # 13.2%
    300 > 196  # 19.6%
    400 > 362  # 36.2%
    500 > 477  # 47.7%
    700 > 732  # 73.2%
    900 > 1000 # 100%
```

## 4. СТРУКТУРА КОДА

### 4.1 Основные классы

```python
# Модель данных
@dataclass
class DSLAxis:
    name: str
    tag: str
    minimum: float
    default: float
    maximum: float
    mappings: List[AxisMapping]
    
@dataclass
class DSLMaster:
    name: str
    filepath: str
    location: Dict[str, float]  # axis_name -> design_value
    flags: Set[str]  # {'base', 'copy_lib', 'copy_features'}
    
@dataclass
class DSLDocument:
    family: str
    axes: List[DSLAxis]
    masters: List[DSLMaster]
    instances: List[DSLInstance]
    rules: List[DSLRule]
```

### 4.2 Парсер DSL

```python
class DSLParser:
    """Многоуровневый парсер с поддержкой разных форматов"""
    
    def parse(self, content: str) -> DSLDocument:
        # 1. Токенизация
        # 2. Определение формата (базовый/компактный/расширенный)
        # 3. Контекстный парсинг по секциям
        # 4. Применение дефолтов
        # 5. Валидация
```

### 4.3 Генератор DesignSpace

```python
class DesignSpaceGenerator:
    """Генерирует .designspace из DSL"""
    
    def generate(self, dsl: DSLDocument) -> DesignSpaceDocument:
        # 1. Создание осей с маппингом
        # 2. Добавление мастеров с правильными location
        # 3. Генерация инстансов (auto или explicit)
        # 4. Применение правил
        # 5. Оптимизация
```

## 5. КЛЮЧЕВЫЕ АЛГОРИТМЫ

### 5.1 Автоопределение мастеров по именам файлов
```python
def parse_master_filename(filename: str) -> Dict[str, Any]:
    """
    CnLt.ufo → {width: 'Compressed', weight: 'Light'}
    ExtendedBold.ufo → {width: 'Extended', weight: 'Bold'}
    """
    # Использовать паттерны из маппинга осей
    # Учитывать разные варианты написания
    # Обрабатывать составные имена
```

### 5.2 Проверка покрытия дизайн-пространства
```python
def analyze_coverage(masters: List[Master], axes: List[Axis]) -> CoverageReport:
    """
    Проверяет:
    - Все ли углы покрыты (обязательно)
    - Есть ли центральные мастера (желательно)
    - Какие веса интерполируются
    - Качество интерполяции (расстояние между мастерами)
    """
```

### 5.3 Автогенерация инстансов
```python
def generate_instances(axes: List[Axis], strategy: str) -> List[Instance]:
    """
    Стратегии:
    - 'all': все комбинации
    - 'standard': только стандартные веса
    - 'sparse': минимальный набор
    """
```

## 6. РАСШИРЕННЫЕ ВОЗМОЖНОСТИ

### 6.1 Поддержка include
```dsl
include common-axes.dsl
include brand-weights.dsl

family MyFont extends CommonBase
```

### 6.2 Переменные и константы
```dsl
$brand-weight = 425
$company-blue = #0066CC

weight
    Regular $brand-weight > 425
```

### 6.3 Условная генерация
```dsl
instances
    if has_italic:
        generate all combinations
    else:
        generate upright only
```

### 6.4 Валидация и предупреждения
```dsl
validate
    ✓ Все углы покрыты
    ⚠️ Bold интерполируется (большое расстояние)
    💡 Рекомендуется добавить Bold мастер
```

## 7. CLI ИНТЕРФЕЙС

```bash
# Основные команды
dsl2designspace convert font.dsl -o font.designspace
dsl2designspace reverse font.designspace -o font.dsl

# Валидация
dsl2designspace validate font.dsl
dsl2designspace coverage font.dsl --visual

# Генерация
dsl2designspace init ./sources/  # создать DSL из папки с UFO
dsl2designspace generate font.dsl --instances all

# Watch режим для разработки
dsl2designspace watch font.dsl --auto-compile
```

## 8. ТЕСТОВЫЕ СЛУЧАИ

### 8.1 Простое семейство (2D)
- 3 веса × 2 стиля = 6 мастеров
- Линейный маппинг
- Стандартные имена

### 8.2 Сложное семейство (3D)
- weight × width × italic
- Нелинейный маппинг
- Разреженная матрица мастеров

### 8.3 Экстремальный случай (4D)
- 72 мастера для 432 инстансов
- Интерполяция целой оси
- Сложные правила подстановки

## 9. СТРУКТУРА ПРОЕКТА

```
dsl2designspace/
├── src/
│   ├── __init__.py
│   ├── parser.py        # DSL парсер
│   ├── generator.py     # DesignSpace генератор
│   ├── models.py        # Модели данных
│   ├── validators.py    # Валидация и анализ
│   ├── defaults.py      # Стандартные значения
│   └── utils.py         # Вспомогательные функции
├── tests/
│   ├── fixtures/        # Тестовые DSL и designspace файлы
│   ├── test_parser.py
│   ├── test_generator.py
│   └── test_roundtrip.py
├── examples/
│   ├── simple.dsl
│   ├── complex.dsl
│   └── kazimir.dsl
└── cli.py              # CLI интерфейс

```

## 10. ПРИОРИТЕТЫ РЕАЛИЗАЦИИ

### Фаза 1: MVP (Минимально работающий продукт)
1. Базовый парсер DSL (простой формат)
2. Генератор DesignSpace с осями и мастерами
3. Поддержка маппинга user/design space
4. CLI для конвертации

### Фаза 2: Основные возможности
1. Поддержка стандартных весов/ширин
2. Автогенерация инстансов
3. Правила подстановки
4. Валидация покрытия

### Фаза 3: Продвинутые возможности  
1. Компактные форматы записи
2. Автоопределение мастеров по именам
3. Include и переменные
4. Визуализация покрытия

### Фаза 4: Оптимизации
1. Определение лишних мастеров
2. Предложения по улучшению
3. Автоматическая оптимизация
4. Экспорт в другие форматы

## 11. ПРИМЕРЫ ИСПОЛЬЗОВАНИЯ

### Пример 1: Создание нового семейства
```bash
# Создаём DSL файл
cat > myfont.dsl << EOF
family MyFont
weight: Light Regular Bold
italic: binary
masters: auto
instances: auto
EOF

# Генерируем designspace
dsl2designspace convert myfont.dsl

# Компилируем в вариативный шрифт
fontmake -m myfont.designspace -o variable
```

### Пример 2: Миграция существующего проекта
```bash
# Конвертируем существующий designspace в DSL
dsl2designspace reverse oldfont.designspace -o newfont.dsl

# Редактируем DSL (убираем лишнее, упрощаем)
vim newfont.dsl

# Генерируем обратно оптимизированный designspace
dsl2designspace convert newfont.dsl --optimize
```

### Пример 3: Анализ покрытия
```bash
# Проверяем покрытие дизайн-пространства
dsl2designspace coverage font.dsl

# Вывод:
# ✓ Углы покрыты: 100%
# ⚠️ Интерполируемые веса: Medium (расстояние: 365), Bold (расстояние: 330)
# 💡 Рекомендации:
#    - Добавить Bold.ufo для улучшения качества
#    - Medium хорошо интерполируется, мастер не обязателен
```

## 12. ВАЖНЫЕ ДЕТАЛИ РЕАЛИЗАЦИИ

### Обработка ошибок
- Чёткие сообщения об ошибках с указанием строки
- Предложения по исправлению
- Graceful degradation при неполных данных

### Производительность
- Ленивая загрузка UFO файлов
- Кэширование результатов парсинга
- Параллельная обработка при сканировании папок

### Совместимость
- Поддержка DesignSpace format 5.0
- Обратная совместимость с format 4.x
- Экспорт в format 3.x при необходимости

### Расширяемость
- Плагины для кастомных форматов
- Hooks для пре/пост процессинга
- API для использования как библиотеки

## ЗАКЛЮЧЕНИЕ

Этот инструмент должен сделать работу с вариативными шрифтами значительно проще и понятнее. Ключевая идея - скрыть сложность XML формата за простым и выразительным DSL, при этом сохранив полную функциональность и добавив умные автоматизации.

Основной принцип: "Convention over Configuration" - работает из коробки для 90% случаев, но позволяет переопределить всё что нужно для оставшихся 10%.
