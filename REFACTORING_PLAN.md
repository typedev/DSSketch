# План рефакторинга DSSketch

**Версия:** 2.0  
**Дата обновления:** 2025-08-10

## 📊 Анализ текущего состояния

### Проблемы
- **Размер файла**: `dssketch.py` содержит 1804 строки кода
- **Монолитность**: Весь код в одном файле затрудняет поддержку и тестирование
- **Дублирование**: Повторяющаяся логика для discrete axes, условий, путей
- **Неиспользуемый код**: Несколько методов и полей не используются

### Обнаруженные повторения
1. Логика определения discrete axes (4+ места)
2. Парсинг и форматирование условий
3. Обработка путей к файлам
4. Преобразование axis name/tag

## 📁 Новая структура проекта (src layout)

```
DSSketch/
├── src/
│   └── dssketch/
│       ├── __init__.py          # Публичный API пакета
│       ├── config.py            # DataManager для управления данными
│       ├── core/
│       │   ├── __init__.py
│       │   ├── models.py        # DSSDocument, DSSAxis, DSSMaster и др.
│       │   ├── validation.py    # UFOValidator, ValidationReport
│       │   └── mappings.py      # UnifiedMappings, Standards
│       ├── parsers/
│       │   ├── __init__.py
│       │   ├── dss_parser.py    # DSSParser
│       │   └── axis_parser.py   # Специализированный парсер осей
│       ├── writers/
│       │   ├── __init__.py
│       │   └── dss_writer.py    # DSSWriter
│       ├── converters/
│       │   ├── __init__.py
│       │   ├── designspace_to_dss.py  # DesignSpaceToDSS
│       │   └── dss_to_designspace.py  # DSSToDesignSpace
│       ├── utils/
│       │   ├── __init__.py
│       │   ├── patterns.py      # PatternMatcher
│       │   ├── paths.py         # Утилиты работы с путями
│       │   ├── conditions.py    # Парсинг и форматирование условий
│       │   └── discrete.py      # Логика discrete axes
│       └── data/                # Встроенные данные (включаются в пакет)
│           ├── unified-mappings.yaml
│           ├── discrete-axis-labels.yaml
│           └── stylenames.json
├── dssketch_cli.py              # Основной CLI
├── dssketch_data_cli.py         # CLI для управления данными
├── pyproject.toml               # Конфигурация пакета
├── MANIFEST.in                  # Контроль файлов в дистрибутиве
├── README.md                    # Документация (включается в пакет)
├── LICENSE                      # Лицензия (включается в пакет)
├── requirements.txt
├── tests/                       # Тесты (НЕ включаются в пакет)
│   ├── __init__.py
│   ├── test_core/
│   ├── test_parsers/
│   └── test_converters/
├── examples/                    # Примеры (НЕ включаются в пакет)
├── docs/                        # Документация разработки (НЕ включается)
│   ├── CLAUDE.md               # Инструкции для Claude Code
│   ├── REFACTORING_PLAN.md     # Этот документ
│   ├── DATA_CUSTOMIZATION.md   # Документация по кастомизации
│   ├── PERFORMANCE.md
│   └── NEXT_STEPS.md
└── .gitignore                   # Git игнор-файл
```

## 🎯 Преимущества src layout

1. **Изоляция кода** - предотвращает случайный импорт из рабочей директории
2. **Чистое тестирование** - тесты всегда используют установленную версию
3. **Стандарт PyPA** - рекомендован Python Packaging Authority
4. **Защита от ошибок** - невозможно импортировать неустановленный пакет
5. **Совместимость с pip** - оптимально для публикации в PyPI

## 🗂️ Управление файлами в пакете

### Что включается в пакет:
- ✅ `src/dssketch/` - весь код Python
- ✅ `src/dssketch/data/` - встроенные файлы данных
- ✅ `README.md` - основная документация
- ✅ `LICENSE` - лицензия

### Что НЕ включается:
- ❌ `docs/` - документация разработки (CLAUDE.md и др.)
- ❌ `tests/` - тесты
- ❌ `examples/` - примеры (опционально)
- ❌ Все `*.md` файлы кроме README.md
- ❌ `.gitignore`, `.vscode/`, и другие файлы разработки

### Контроль через MANIFEST.in:
```ini
# Включить
include LICENSE
include README.md
recursive-include src/dssketch/data *.json *.yaml

# Исключить
exclude *.md
exclude CLAUDE.md
prune tests
prune docs
```

## 🔧 Конкретные рефакторинги

### 1. Выделение общей логики discrete axes

```python
# src/dssketch/utils/discrete.py
class DiscreteAxisHandler:
    """Централизованная обработка discrete axes"""
    
    DISCRETE_AXES = ['italic', 'ital', 'slant', 'slnt']
    
    @staticmethod
    def is_discrete(axis) -> bool:
        """Единая проверка на discrete axis"""
        return (axis.minimum == 0 and 
                axis.default == 0 and 
                axis.maximum == 1 and
                axis.name.lower() in DiscreteAxisHandler.DISCRETE_AXES)
    
    @staticmethod
    def load_labels() -> Dict[str, Dict[int, List[str]]]:
        """Загрузка меток для discrete axes из YAML"""
        # Перенести из DSSParser._load_discrete_labels
```

### 2. Унификация работы с условиями

```python
# src/dssketch/utils/conditions.py
class ConditionHandler:
    """Централизованная обработка условий правил"""
    
    @staticmethod
    def parse(condition_str: str) -> List[dict]:
        """Единый парсер условий"""
        # Логика из DSSParser._parse_condition_string
    
    @staticmethod
    def format(conditions: List[dict]) -> str:
        """Единый форматтер условий"""
        # Логика из DSSWriter._format_rule
```

### 3. Управление путями

```python
# src/dssketch/utils/paths.py
class PathResolver:
    """Утилиты для работы с путями к файлам"""
    
    @staticmethod
    def resolve_master_path(master_filename: str, base_path: str) -> Path:
        """Разрешение пути к мастер-файлу"""
        # Общая логика из разных мест
    
    @staticmethod
    def find_common_directory(paths: List[Path]) -> Optional[Path]:
        """Поиск общей директории для файлов"""
        # Логика из DesignSpaceToDSS._determine_masters_path
```

### 4. Управление данными конфигурации

```python
# src/dssketch/config.py
class DataManager:
    """Управление данными с поддержкой пользовательских модификаций"""
    
    def __init__(self):
        # Встроенные данные в пакете
        self.package_data_dir = Path(__file__).parent / "data"
        
        # Пользовательские данные (по ОС)
        self.user_data_dir = self._get_user_data_dir()
        
    def load_data_file(self, filename: str) -> Dict:
        """Загрузка с приоритетом: user → package → empty"""
        # Сначала проверяем пользовательский файл
        # Затем fallback на встроенный
        # Возвращаем пустой dict если ничего не найдено
    
    def reset_to_defaults(self, file_name: Optional[str] = None):
        """Сброс к дефолтным настройкам"""
```

**Расположение пользовательских данных:**
- macOS: `~/Library/Application Support/dssketch/`
- Linux: `~/.config/dssketch/`
- Windows: `%APPDATA%\dssketch\`

### 5. Разделение больших классов

- **DSSParser** (430 строк) → разделить на:
  - `AxisParser` - парсинг осей
  - `MasterParser` - парсинг мастеров
  - `RuleParser` - парсинг правил
  - `InstanceParser` - парсинг инстансов

- **DSSWriter** (310 строк) → разделить на:
  - `AxisFormatter` - форматирование осей
  - `RuleFormatter` - форматирование правил
  - `PatternOptimizer` - оптимизация паттернов

## 📦 Конфигурация для PyPI

### pyproject.toml

```toml
[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "dssketch"
version = "1.0.0"
description = "Compact, human-readable format for DesignSpace files"
authors = [{name = "Your Name", email = "your.email@example.com"}]
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.8"
keywords = ["fonts", "designspace", "variable-fonts", "typography"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "Topic :: Text Processing :: Fonts",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.8",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
]
dependencies = [
    "fonttools>=4.38.0",
    "defcon>=0.10.0",
    "fontParts>=0.12.0",
    "designspaceProblems>=0.1.0",
    "pyyaml>=6.0",
]

[project.urls]
Homepage = "https://github.com/yourusername/dssketch"
Documentation = "https://github.com/yourusername/dssketch#readme"
Repository = "https://github.com/yourusername/dssketch.git"
Issues = "https://github.com/yourusername/dssketch/issues"

[project.scripts]
dssketch = "dssketch_cli:main"
dss = "dssketch_cli:main"  # Короткий алиас
dssketch-data = "dssketch_data_cli:main"  # Управление данными

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "pytest-cov>=4.0",
    "black>=23.0",
    "mypy>=1.0",
    "ruff>=0.1.0",
]

[tool.setuptools]
include-package-data = false  # Не включать все файлы автоматически

[tool.setuptools.packages.find]
where = ["src"]
include = ["dssketch*"]
exclude = ["tests*", "docs*"]

[tool.setuptools.package-data]
dssketch = ["data/*.json", "data/*.yaml"]

[tool.setuptools.exclude-package-data]
"*" = [
    "*.md",
    "CLAUDE.md",
    "*.txt",
    ".gitignore",
    "*.pyc",
    "__pycache__",
]
```

## 🚀 План миграции

### Фаза 1: Подготовка структуры
1. Создать директории `src/dssketch/` и поддиректории
2. Переместить `*.md` файлы разработки в `docs/`
3. Создать `MANIFEST.in` для контроля дистрибутива
4. Настроить `pyproject.toml` с правильными исключениями

### Фаза 2: Управление данными
1. Создать `config.py` с классом `DataManager`
2. Перенести файлы данных в `src/dssketch/data/`
3. Обновить загрузку данных для поддержки user/package приоритета
4. Создать CLI утилиту `dssketch-data` для управления данными

### Фаза 3: Выделение моделей и утилит
1. Перенести dataclasses в `core/models.py`
2. Выделить `UFOValidator` в `core/validation.py`
3. Перенести `UnifiedMappings` в `core/mappings.py`
4. Создать утилиты в `utils/` (discrete, conditions, paths, patterns)

### Фаза 4: Разделение конвертеров
1. Перенести `DesignSpaceToDSS` в `converters/designspace_to_dss.py`
2. Перенести `DSSToDesignSpace` в `converters/dss_to_designspace.py`
3. Разделить `DSSParser` на модули в `parsers/`
4. Разделить `DSSWriter` на модули в `writers/`

### Фаза 5: Интеграция и тестирование
1. Обновить импорты во всех модулях
2. Создать публичный API в `src/dssketch/__init__.py`
3. Обновить `dssketch_cli.py` для использования новой структуры
4. Провести тестирование всех примеров из `examples/`

### Фаза 6: Очистка и документация
1. Удалить неиспользуемый код
2. Добавить type hints для всех публичных методов
3. Написать/обновить docstrings
4. Создать тесты для каждого модуля
5. Обновить README.md с новой структурой импортов

## 📈 Ожидаемые результаты

- **Уменьшение размера основного файла**: с 1804 до ~300 строк
- **Улучшение тестируемости**: модульная структура упрощает написание тестов
- **Повышение читаемости**: логическое разделение по функциям
- **Упрощение поддержки**: изменения локализованы в соответствующих модулях
- **Готовность к PyPI**: структура соответствует стандартам Python-пакетов

## 🔐 Управление пользовательскими данными

### CLI утилита dssketch-data

```bash
# Показать информацию о расположении файлов
dssketch-data info

# Открыть папку с пользовательскими данными
dssketch-data edit

# Сбросить все файлы к дефолтным
dssketch-data reset --all

# Сбросить конкретный файл
dssketch-data reset --file unified-mappings.yaml

# Показать путь к пользовательским данным
dssketch-data path
```

### Приоритет загрузки данных

1. **Пользовательские файлы** (если существуют)
   - macOS: `~/Library/Application Support/dssketch/`
   - Linux: `~/.config/dssketch/`
   - Windows: `%APPDATA%\dssketch\`

2. **Встроенные файлы** из пакета (`src/dssketch/data/`)

3. **Пустой словарь** если ничего не найдено

### Переменные окружения

- `DSSKETCH_DATA_DIR` - кастомный путь к данным

## 🛠️ Инструменты для рефакторинга

### Для анализа кода
```bash
# Проверка сложности
radon cc dssketch.py -s

# Поиск дублирования
pylint --disable=all --enable=duplicate-code dssketch.py

# Анализ зависимостей
pydeps dssketch.py
```

### Для форматирования
```bash
# Форматирование кода
black src/

# Сортировка импортов
isort src/

# Проверка типов
mypy src/
```

### Для тестирования
```bash
# Запуск тестов
pytest tests/

# Покрытие кода
pytest --cov=src/dssketch tests/
```

## 📝 Примечания

1. **Обратная совместимость**: 
   - Сохранить алиас `Standards = UnifiedMappings`
   - Поддержка старого вызова `python dssketch.py`

2. **Миграция данных**: 
   - Встроенные данные в `src/dssketch/data/`
   - Автоматическое создание пользовательской директории при первом запуске
   - DataManager обеспечивает прозрачную загрузку

3. **CLI утилиты**:
   - `dssketch` - основной конвертер
   - `dss` - короткий алиас
   - `dssketch-data` - управление данными

4. **Исключение из пакета**:
   - CLAUDE.md и другие MD файлы разработки остаются в репозитории
   - Используются только для разработки, не попадают в pip-пакет
   - Контролируется через MANIFEST.in

5. **Документация**: 
   - README.md включается в пакет
   - Обновить с новыми примерами импорта
   - Добавить раздел про кастомизацию данных

## ✅ Критерии успеха

- [ ] Все тесты проходят после рефакторинга
- [ ] Примеры из `examples/` работают корректно
- [ ] Пакет устанавливается через `pip install -e .`
- [ ] CLI утилиты работают после установки:
  - [ ] `dssketch` - основной конвертер
  - [ ] `dss` - короткий алиас
  - [ ] `dssketch-data` - управление данными
- [ ] Пользовательские данные корректно загружаются
- [ ] CLAUDE.md и другие MD файлы НЕ включаются в пакет
- [ ] Документация обновлена с новой структурой
- [ ] Код проходит линтеры (black, ruff, mypy)
- [ ] Размер основного файла уменьшен до ~300 строк
- [ ] Пакет готов к публикации в PyPI

## 📅 Временная оценка

- **Фаза 1**: 1-2 часа (структура и конфигурация)
- **Фаза 2**: 2-3 часа (управление данными)
- **Фаза 3**: 2-3 часа (выделение моделей)
- **Фаза 4**: 3-4 часа (разделение конвертеров)
- **Фаза 5**: 2-3 часа (интеграция и тестирование)
- **Фаза 6**: 2-3 часа (очистка и документация)

**Итого**: ~15-20 часов на полный рефакторинг

## 🎉 Результат

После завершения рефакторинга:
- Проект будет иметь профессиональную структуру
- Код станет модульным и тестируемым
- Пакет будет готов к публикации в PyPI
- Пользователи смогут легко кастомизировать данные
- Разработка новых функций станет проще